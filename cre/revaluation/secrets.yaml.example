# CRE secrets manifest (template) — ILLUSTRATIVE ONLY for this slice.
#
# CRE-01a needs NO live secret (K5): the §8.9 mock re-appraisal batch arrives via the http trigger payload,
# not via a fetched/authenticated feed. This file is a placeholder so the module matches the scaffold shape.
#
# When the real Proof-of-Value feed swaps in (§8.10), a DON-only key (e.g. a feed API token) would be read in
# the HANDLER (onReappraisal), NEVER inside the node-mode `observe` (§8.1 forbids secrets in a consensus
# observation). At that point add the secret here and key it to the handler read.
secretsNames: {}

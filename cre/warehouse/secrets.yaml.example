# CRE secrets manifest (template) — ILLUSTRATIVE ONLY for this slice.
#
# CRE-04 needs NO live secret (K6): the §8.5/§8.9 mock warehouse-op event (the op + the pre-computed
# amount/shares/dest) arrives via the http trigger payload, not via a fetched/authenticated feed. This file is a
# placeholder so the module matches the scaffold shape.
#
# The §8.5 on-chain NAV sizing (the production replacement of `observe`) reads NO secret — it is a per-node
# evmClient.CallContract. A future authenticated off-chain shortfall feed WOULD read a DON-only key; that key is
# read in the HANDLER (onWarehouseOp), NEVER inside the node-mode `observe` (§8.1/§8.5 forbid secrets in a
# consensus observation). At that point add the secret here and key it to the handler read.
secretsNames: {}

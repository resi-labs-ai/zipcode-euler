# CRE secrets manifest (template) — ILLUSTRATIVE ONLY for this slice.
#
# CRE-01b needs NO live secret (K6): the §8.9 mock Proof gates + equity mark arrive via the http trigger
# payload, not via a fetched/authenticated feed. This file is a placeholder so the module matches the
# scaffold shape.
#
# When the real Proof / Plaid / Credit-Karma / Pippin / DART / Block-Analitica feeds swap in (§8.10), a
# DON-only key (e.g. a feed API token) would be read in the HANDLER (onApplication), NEVER inside the
# node-mode `observe` (§8.1 forbids secrets in a consensus observation). At that point add the secret here
# and key it to the handler read.
secretsNames: {}

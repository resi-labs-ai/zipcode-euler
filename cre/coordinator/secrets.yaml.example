# CRE secrets manifest (template) — ILLUSTRATIVE ONLY for this slice.
#
# CRE-01c needs NO live secret (K6): the §8.4/§8.9 mock loss event (the loss magnitudes + the capital-vs-premium
# split) arrives via the http trigger payload, not via a fetched/authenticated feed. This file is a placeholder
# so the module matches the scaffold shape.
#
# When the real recovery / foreclosure / insurance feeds swap in (§8.4/§8.9), a DON-only key (e.g. a feed API
# token) would be read in the HANDLER (onLossEvent), NEVER inside the node-mode `observe` (§8.1/§8.4 forbid
# secrets + PII in a consensus observation). At that point add the secret here and key it to the handler read.
secretsNames: {}

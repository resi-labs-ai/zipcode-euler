# 2.md — Milestone-1 tx-by-tx acceptance script

> **PLAIN NAME: the where-money-flows check** — proof the whole money flow works start to finish.
> Quarantined build scaffolding. Dies once item 10's integration tests cover the same ground (see `deathnote.md`).


> **Read me first.** This is the executable definition of "M1 done." You — the Claude picking this up —
> use it with `claude-zipcode.md` open, and either (a) execute it tx-by-tx on **Base mainnet** (8453) with
> Foundry + cast, or (b) translate each step into a Foundry test that asserts the same post-state (on a Base
> **mainnet fork** — the Baal/Safe/Zodiac/ICHI/Hydrex deps are 8453-only). Either
> way, the deliverable is an **execution log** showing every L-step's post-state assertions pass and
> every N-step reverts with the expected error. No new contract design happens here — if a step needs a
> mechanism the spec doesn't define, that's a finding, not a license to invent.
>
> Out of scope (do not test, do not extend): default flow / xALPHA slash / recovery (M2), the secondary
> AMM (§6.2), multiple originators, perspective verification (§17/§19 dropped).

---

## 1. Scope

- **Chain:** Base **mainnet** (8453). **Selector:** `ethereum-mainnet-base-1`. (Deploy + test on mainnet — the
  item-8 vault deps are 8453-only; see `tickets/PROGRESS.md` deploy-target decision.)
- **Actors (test EOAs):** `CONTROLLER_OWNER` (deployer + initial owner; renounces post-wiring),
  `LP_A`, `LP_B` (suppliers; two so the redemption pro-rata path is non-trivial), `JR` (junior depositor —
  Baal Loot holder), `ORIGINATOR` (off-chain only — wires USD via Erebor; never touches the chain).
- **Loop:** one originator, one lien, full round-trip: senior deposit → mint zipUSD; junior deposit → Loot;
  originate → draw → repay → close → senior redeem → junior ragequit. **(Junior leg reframed 2026-06-06: the
  old convert-on-stake `stake` / 30-day `cooldown` / `unstake` is DELETED. Junior deposit = the 8-B2
  deposit/mint shaman [zipUSD → Loot into the Safe]; junior exit = Baal `ragequit` [in-kind] gated by the 8-B3
  lock/freeze shaman. The senior-side deposit/custody/yield-routing wiring re-aligns with 8-S2b two-Safe
  custody, in design. The junior steps below are excised + marked, not rewritten — author them with the 8-B
  tickets.)**
- **Collateral is mocked** (SPV proof open per `spv-lien-proof.md`).

---

## 2. Symbol table (fill the addresses in your run log)

| Symbol | Contract | Notes |
|---|---|---|
| `EVC` | `EthereumVaultConnector` (canonical Base mainnet `0x5301c7dD20bD945D2013b48ed0DEE3A284ca8989`) | reused |
| `EE_POOL` | `EulerEarn` (deployed via `EulerEarnFactory`) | reused — USDC pool |
| `USDC` | Base mainnet USDC `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913` (**real** USDC — fund via `deal()`/whale on the fork; no testnet faucet) | reused |
| `FORWARDER` | Chainlink CRE KeystoneForwarder (Base mainnet `0xF8344CFd5c43616a4366C34E3EEE75af79a74482`) | reused, address fixed at deploy |
| `TIMELOCK` | OpenZeppelin `TimelockController` | net-new; deployed in Phase S |
| `ZIP_ORACLE_REG` | `ZipcodeOracleRegistry is ReceiverTemplate, BaseAdapter` | net-new |
| `LIEN_FACTORY` | `LienTokenFactory` (CREATE2) | net-new |
| `LIEN_i` | `LienCollateralToken` (CREATE2: `salt = keccak256(abi.encode(lienId))`) | net-new; precomputed before L4 |
| `EVAULT_i` | isolated EVK market for `LIEN_i` (USDC borrow, `LIEN_i` collateral) | reused (`GenericFactory.createProxy`) |
| `GATING_HOOK` | `CREGatingHook is IHookTarget` | net-new |
| `VENUE` | `EulerVenueAdapter is IZipcodeVenue` — holds the Euler roles (allocator, market/router governor, hook owner, EVC sub-accounts) | net-new (config one; §4.7) |
| `ZIP_CONTROLLER` | `ZipcodeController is ReceiverTemplate` | net-new |
| `ZIPUSD` | `ESynth` (zipUSD) | reused contract, deployed instance |
| `SZIPUSD` | `szipUSD` junior — **Baal Loot on a Safe basket** (zipUSD + xALPHA + LP); ragequit in-kind (§2/§4.5). *(The convert-on-stake ERC-4626 vault was deleted 2026-06-06; the junior deposit/exit acceptance is authored at 8-B1/8-B2/8-B3, not in this script.)* | net-new (Baal + Zodiac) |
| `DEPOSIT_MODULE` | `ZipDepositModule` | net-new |
| `REDEMPTION_QUEUE` | `ZipRedemptionQueue` (fork of MIT `erc7540-reference`) | net-new |
| `EREBOR` | mocked off-ramp address (an EOA in this test) | mocked |

Borrow-account convention (borrower-model rework, 2026-06-05): each line's borrow account is a **fresh
per-line EVC account** — sub-account 1 of the line's `LineAccount` prefix (the `LineAccount` is CREATE2-deployed
by the venue adapter inside `openLine`, salt = `lienId`, and grants `VENUE` the operator bit over that
code-free sub-account). Symbol: `LINE_BORROW_ACCOUNT` (per line). There is **no** controller sub-account and
**no** `ZIP_CONTROLLER_SUB` — that was the retired sub-account scheme (§4.4/§4.7).

---

## 3. Phase S — one-time setup & wiring (do all of this once, then loop)

Each step format:
```
S<n>. <name>
  Caller:        <EOA/contract>
  Via:           (direct | EVC.batch | timelock.schedule+execute)
  Target.fn:     Contract.function(args…)
  Expected events:  [event(s)]
  Post-state:    [assertions]
  Notes:         [gotchas]
```

### S1. Deploy USDC test pool wiring (assume USDC + EVC already on Base mainnet)
- Caller: `CONTROLLER_OWNER`
- Direct: deploy `EE_POOL` via `EulerEarnFactory.createEulerEarn(USDC, ...)`.
- Post: `EE_POOL.asset() == USDC`; `EE_POOL.totalSupply() == 0`.

### S2. Deploy TimelockController
- Caller: `CONTROLLER_OWNER`
- Direct: `new TimelockController(2 days, proposers=[future controller], executors=[address(0)], admin=CONTROLLER_OWNER)`. After S8, admin renounces.
- Post: `TIMELOCK.getMinDelay() == 2 days`.

### S3. Deploy ZIP_ORACLE_REG
- Caller: `CONTROLLER_OWNER`
- Direct: `new ZipcodeOracleRegistry(FORWARDER, USDC, validityWindow=long_line_term)` — the mark is event-driven, so the window is **long (line-term)**, not a short staleness clock (§4.1). (The base `setForwarderAddress` is non-virtual → not overridden; the Forwarder is sealed immutable by the S11 renounce, not here, §4.1/§4.4.)
- Post: `ZIP_ORACLE_REG.getForwarderAddress() == FORWARDER`. (At S3 the deployer-owner can still call `setForwarderAddress` — immutability is sealed at S11 post-renounce, where the call reverts `OwnableUnauthorizedAccount`.)

### S4. Deploy LIEN_FACTORY
- Caller: `CONTROLLER_OWNER`
- Direct: `new LienTokenFactory(...)`. Confirm CREATE2 salt formula is `keccak256(abi.encode(lienId))` and decimals pinned at 18.
- Post: `LIEN_FACTORY.LIEN_DECIMALS() == 18`.

### S5. Deploy GATING_HOOK
- Caller: `CONTROLLER_OWNER`
- Direct: `new CREGatingHook(EVK_FACTORY, EVC, VENUE_precomputed)` (three immutables: the EVK `GenericFactory`, the EVC, the **`borrowDriver`** = the **`VENUE`** adapter — the `EVC.call` caller, **not** the controller). The hook's gated address must be the adapter (S6); since the adapter isn't deployed until S6 (circular immutables), **precompute `VENUE` via CREATE2** and pass that, or deploy the adapter first / two-pass — confirm the deployed `VENUE` matches the precomputed address before proceeding. Confirm the fallback uses `EVC.isAccountOperatorAuthorized(caller, VENUE)` (operator-authorization, not an owner check or equality — each line borrows on a fresh per-line account, **borrow-driver/`VENUE`-as-operator**, §4.3/§4.4). (In M1, if the adapter is collapsed into the controller, `VENUE == ZIP_CONTROLLER` — same address, still correct.)
- Post: `GATING_HOOK.isHookTarget() == 0x87439e04` (the IHookTarget selector).

### S6. Deploy VENUE adapter + ZIP_CONTROLLER
- Caller: `CONTROLLER_OWNER`
- Direct: `new EulerVenueAdapter(ZIP_CONTROLLER, EVC, EE_POOL, EVK_FACTORY, ZIP_ORACLE_REG, GATING_HOOK, IRM, USDC, EREBOR)`
  (holds the Euler roles; **no shared-router or TIMELOCK arg — each line's router is minted + frozen inside
  `openLine`**, §4.1/§4.7); then `new ZipcodeController(FORWARDER, VENUE, LIEN_FACTORY, ZIP_ORACLE_REG, EREBOR)`
  (**5-arg** — `EREBOR` is the draw off-ramp the controller passes to `venue.draw`, §4.4; the controller does
  not take the EVC as an immutable). (The base `setForwarderAddress` is non-virtual → not overridden;
  immutability is sealed at S11 by renounce, §4.1/§4.4.) (M1 may collapse the adapter into the controller as
  long as the `IZipcodeVenue` seam holds, §4.4.)
- Then: `ZIP_ORACLE_REG.setController(ZIP_CONTROLLER)` (set-once, `onlyOwner`) — wires the registry's
  origination-seed authority now that the controller exists (the registry deployed first at S3, so this is a
  setter, not a constructor immutable, §4.1).
- **No controller-level operator wiring (REMOVED by the borrower-model rework, 2026-06-05).** The old
  `ZIP_CONTROLLER.wireVenueOperator(EVC)` / blanket `setOperator(prefix, VENUE, ~uint256(1))` step is **gone** —
  the operator grant is now issued **per line at origination** by the adapter's `LineAccount`
  (`EVC.setAccountOperator(borrowAccount, VENUE, true)` inside `openLine`, §4.4/§4.7), so the controller takes no
  EVC handle and there is no deploy-time wiring `draw` depends on. (Each line's borrow account is a fresh
  `LineAccount` prefix, not a controller sub-account.)
- Post: all immutables match; `ZIP_CONTROLLER.getForwarderAddress() == FORWARDER`; `ZIP_CONTROLLER.venue() == VENUE`; `ZIP_ORACLE_REG.controller() == ZIP_CONTROLLER`. (Per-line operator authorization is asserted at L4, not here — each line's `LineAccount` grants `VENUE` over its own `borrowAccount` inside `openLine`.)

### S7. Deploy supply-side stack
- Direct: `new ZIPUSD (ESynth(EVC, "Zipcode USD", "zipUSD"))` — **`ESynth` ctor is `(address evc, string name,
  string symbol)`, 3-arg, NO decimals param; zipUSD reports `decimals() == 18`** (OZ ERC20 default,
  `ESynth.sol:36-41`). `new DEPOSIT_MODULE(ZIPUSD, USDC, EE_POOL)`; `new REDEMPTION_QUEUE(ZIPUSD, EE_POOL,
  ZIP_CONTROLLER)`.
- Wire: `ZIPUSD.setCapacity(DEPOSIT_MODULE, type(uint128).max)` (module mints zipUSD on deposit).
- Then **renounce `ESynth` (ZIPUSD) ownership** so no new minter can ever be granted (§9; freezes the
  owner-only surface — `setCapacity`/`allocate`/`deallocate`). **Timing caveat (new model):** if the Baal
  loss-side (lever-1 zipUSD burn, §11/§4.6, M2) requires an ESynth authority, the renounce must wait until that
  minter/burner set is final — settle at 8-S2b/8-B before asserting `owner() == address(0)`.
- Post: `ZIPUSD.minters[DEPOSIT_MODULE] == max`; `ZIPUSD.decimals() == 18`. (`ZIPUSD.owner() == address(0)`
  only once the minter set is final — see the renounce caveat.)

> **EXCISED (2026-06-06, 8-S3 cleanup) — convert-on-stake junior stack deleted.** The deleted text built
> szipUSD as an `ESynth` minter (`new SZIPUSD(ZIPUSD, EE_POOL, DEPOSIT_MODULE)` as the "EE-share source for
> `stake`"), granted it `max` mint capacity (stake-burn / unstake-mint, with the "capacity MUST be `max`
> exit-wave" rationale), and wired `DEPOSIT_MODULE.setStakingVault(SZIPUSD)` + a standing `EE_POOL` allowance so
> `stake` could pull pool shares from the module. **The Baal szipUSD holds a zipUSD/xALPHA/LP Safe basket —
> never EE pool shares — and is entered by the 8-B2 deposit/mint shaman (zipUSD → Loot), not convert-on-stake.**
> Per the two-Safe custody decision (**8-S2b — DONE**, §4.5 `CreditWarehouse`) the EE pool shares live in a
> **senior custody Safe** (`WAREHOUSE_SAFE`), so the EE-share-transfer + max-allowance seam **dissolves**
> entirely. Author the replacement Phase-S wiring at: **8-Bw** (the `CreditWarehouse` Safe + `WarehouseAdminModule`
> deploy/`enableModule` + the deposit→`WAREHOUSE_SAFE` reshape — the **senior-custody build**) and **8-B1/8-B2**
> (Baal scaffold + deposit/mint shaman — the **junior**). **Do not deploy or test the deleted convert-on-stake
> szipUSD.**

**Decimals convention (verified against `reference/`, applies to all of Phase L below).** USDC = **6 dp**;
zipUSD (`ESynth`) = **18 dp** (fixed); `EE_POOL` shares over USDC = **6 dp** (`ERC4626` offset 0,
`VIRTUAL_AMOUNT = 1e6` → shares ≈ assets at par). "Mint 1:1" is **value**-1:1: `deposit(usdc)` mints
`usdc * 1e12` zipUSD. So zipUSD amounts below are in **e18**, EE-share amounts in **e6**, and the I-sanity
checks compare in **normalized dollars** (`zipUSD/1e18`, `USDC/1e6`, `shares/1e6`), not raw units.

### S8. EulerEarn wiring (VENUE adapter as allocator **+ curator**; perf-fee recipient = protocol-side)
- Caller: `CONTROLLER_OWNER`
- Direct: `EE_POOL.setIsAllocator(VENUE, true)`; **`EE_POOL.setCurator(VENUE)`** (the per-line market
  onboarding — `submitCap`/`acceptCap`/`setSupplyQueue` — happens inside `VENUE.openLine`, §4.7);
  `EE_POOL.setFeeRecipient(<protocol-side fee holder — wired at 8-Bw>)`; `EE_POOL.setFee(f)` (default `f =
  0.5e18`). **Yield routing changed (§5/§17): the lending perf-fee is the PROTOCOL's** (privatized → treasury
  → xALPHA), **NOT** szipUSD's — so the recipient is a protocol-side holder, **not** the junior. The recipient
  policy is fixed by **8-S2b (DONE, §5/§17)**; the concrete recipient address (a protocol holder / the
  `CreditWarehouse` Safe / the treasury) is **wired at the 8-Bw build**. **EE_POOL must be deployed (S1) with
  curator timelock 0** for M1 so
  `submitCap`→`acceptCap` is atomic inside `openLine` (`setTimelock` enforces a 1-day floor; timelock 0 is
  only reachable at `initialize`). Single-curator + zero timelock is an M1 simplification — production-hardening
  (multisig curator + non-zero timelock).
- Post: `EE_POOL.isAllocator(VENUE) == true`; `EE_POOL.curator() == VENUE`; `EE_POOL.feeRecipient() ==
  <protocol-side fee holder, wired at 8-Bw>` (was `SZIPUSD` under the deleted model); `EE_POOL.fee() == f`;
  `EE_POOL.timelock() == 0`.

### S9. (SUBSUMED into `VENUE.openLine` at origination — no standalone up-front market) ~~Deploy isolated market EVAULT_i~~
- **The per-line isolated market is created at origination inside `VENUE.openLine` (L4), not as a standalone
  setup step** (§4.7 revision this session — per-line factory). `openLine` mints, atomically: the escrow
  collateral vault (asset = `LIEN_i`), the USDC borrow vault (`createProxy(0, false, abi.encodePacked(USDC,
  ROUTER_i, USDC))` with `setHookConfig(GATING_HOOK, OP_BORROW|OP_LIQUIDATE)` repay-ungated +
  `setInterestRateModel(IRM)`, governor = VENUE), and the **per-line `ROUTER_i`** (wired `escrowVault→LIEN_i→
  ZIP_ORACLE_REG`, then frozen). It then onboards the borrow vault to `EE_POOL` (`submitCap`/`acceptCap`/
  `setSupplyQueue`, curator = VENUE, timelock 0).
- Post (asserted in L4): `EVAULT_i.governorAdmin() == VENUE`; `EVAULT_i.hookConfig() == (GATING_HOOK,
  OP_BORROW|OP_LIQUIDATE)`, `OP_REPAY` unset; `EVAULT_i.oracle() == ROUTER_i`; `ROUTER_i.governor() ==
  address(0)` (frozen).

### S10. (No shared router to wire) — deploy TIMELOCK for §17 parameter governance only
- **There is no shared `EulerRouter` and no `govSetFallbackOracle`** (§4.1/§4.7 revision): each line's
  `ROUTER_i` is minted, wired to `ZIP_ORACLE_REG`, and frozen inside `openLine` (L4). The price path is
  immutable per line — nothing to transfer to a timelock.
- Caller: `CONTROLLER_OWNER`
- Direct: deploy the OZ `TIMELOCK` (delay ≈2d) for **§17 parameter governance** (szipUSD floor / `f` /
  lock-length setters route through it); `TIMELOCK.grantRole(PROPOSER_ROLE, <governance>)`;
  `TIMELOCK.grantRole(EXECUTOR_ROLE, address(0))`; revoke `CONTROLLER_OWNER`'s admin role.
- Post: the §17 param setters are owned by `TIMELOCK`; **no router governance exists to assert** (per-line
  routers are frozen — see L4 `ROUTER_i.governor() == address(0)`).

### S10b. Set workflow-identity expectations on every ReceiverTemplate subclass — **before** S11
- Caller: `CONTROLLER_OWNER`
- Direct: on each of `ZIP_CONTROLLER`, `ZIP_ORACLE_REG` (and `DEFAULT_COORD` if deployed):
  `setExpectedAuthor(WORKFLOW_OWNER)` (`ReceiverTemplate.sol:143`);
  `setExpectedWorkflowId(WORKFLOW_ID)` (`:184`).
- Why: the identity check in `onReport` (`:88-117`) is enforced **only** when these expected values are
  non-zero. If S11 runs first, ownership is renounced and the setters revert; the identity check is
  permanently bypassed and any workflow the Forwarder accepts can call `onReport`.
- Post: `getExpectedAuthor()` and `getExpectedWorkflowId()` return the wired values on each receiver.

### S11. Renounce Ownable on ZIP_CONTROLLER and ZIP_ORACLE_REG (only after S10b)
- Caller: `CONTROLLER_OWNER`
- **Pre-gate (hard):** assert `getExpectedWorkflowId() != bytes32(0)` on **each** receiver, **abort the
  deploy otherwise** — renouncing with the identity unset permanently bypasses the workflow-identity check
  (any Forwarder-relayed workflow could then write prices/reports), §4.1-F7/§4.4. Also assert the registry's
  `controller != address(0)` (S6 set-once `setController` ran) — post-renounce it can never be set.
- Direct: `ZIP_CONTROLLER.renounceOwnership()`; `ZIP_ORACLE_REG.renounceOwnership()`.
- Post: both `.owner() == address(0)`; the inherited `onlyOwner` setters (`setForwarderAddress`,
  `setExpectedAuthor`, `setExpectedWorkflowId`, registry `setController`) now revert
  `OwnableUnauthorizedAccount`. Seals the immutable-Forwarder + frozen workflow-identity invariants from
  §4.4 (the base setters are non-virtual; renounce is the lock).

### S12. Fund actors with USDC
- Caller: fund via `deal()` / a USDC whale on the fork (**real** Base-mainnet USDC — no testnet faucet)
- LP_A: 1,000,000 USDC; LP_B: 200,000 USDC; JR: 0 (gets zipUSD from LP_A); ORIGINATOR: 0 (off-chain).

**Phase S complete when:** every assertion above holds; no contract owns mutable forwarder/governor
authority that wasn't intended.

---

## 4. Phase L — main loop (12 steps)

Pre-conditions across the loop: define `f = 0.5e18`, **`borrowLTV = 0.8e4`, `liqLTV = 0.85e4`** (EVK
`setLTV` is **1e4 scale**, `uint16` — `IEVault.sol:489-492`; NOT `0.8e18`), `drawAmount = 100_000e6`,
`equityMark = 200_000e6` (so `drawAmount ≤ borrowLTV × equityMark`).

### L1. LP_A supply
- Caller: `LP_A`
- Direct: `USDC.approve(DEPOSIT_MODULE, 1_000_000e6)`; `DEPOSIT_MODULE.deposit(1_000_000e6)`.
- Expected events: `Transfer (USDC: LP_A → DEPOSIT_MODULE)`; `Transfer (zipUSD: 0 → LP_A, 1_000_000e18)` (`usdc * 1e12`); `Deposit (EE_POOL: DEPOSIT_MODULE, 1_000_000e6 assets, ≈1_000_000e6 shares)`; `Deposited(LP_A, 1_000_000e6, 1_000_000e18)`.
- Post-state:
  - `ZIPUSD.balanceOf(LP_A) == 1_000_000e18` OK (value-1:1, 18-dp)
  - `ZIPUSD.totalSupply() == 1_000_000e18` OK
  - `EE_POOL.balanceOf(DEPOSIT_MODULE) ≈ 1_000_000e6` (the module is the protocol-side share custodian, §4.5)
  - `USDC.balanceOf(LP_A) == 0` OK
  - **I1 sanity (new model, `1-results.md` I1):** senior solvency `NAV_s / Z ≥ 1` — here `NAV_s = pool_assets
    = 1,000,000` and `Z = zipUSD_supply = 1,000,000` (normalized dollars) ⇒ `R = 1.0`. *(Replaces the old
    `A = Z + ν + σ` four-bucket check — there is no `ν = J×p` junior pool-share NAV in the Baal model.)*

### L2. LP_B supply
- Caller: `LP_B`
- Direct: `USDC.approve(DEPOSIT_MODULE, 200_000e6)`; `DEPOSIT_MODULE.deposit(200_000e6)`.
- Post: `ZIPUSD.balanceOf(LP_B) == 200_000e18`; total zipUSD supply = 1,200,000e18; pool_assets = 1,200,000e6 (1.2M dollars).

### L3. JR junior deposit (Baal Loot)
> **EXCISED (2026-06-06, 8-S3) — convert-on-stake `stake` deleted.** The old step had JR `SZIPUSD.stake(...)`:
> burn JR's zipUSD (`ESynth.burn`) + transfer ≈100,000e6 EE pool shares from the module into szipUSD → mint
> szipUSD. **The Baal model has no `stake`, no zipUSD burn-on-deposit, and no EE-share custody in the junior.**
> JR deposits zipUSD into the Safe via the **8-B2 deposit/mint shaman → receives Loot**; the deposited zipUSD
> stays in the Safe basket (`b_zip += deposit`) and `Z` is **unchanged** (the zipUSD moves wallet → Safe, still
> circulating + senior-backed). The post-state (Loot minted; basket grows; senior solvency `NAV_s/Z ≥ 1`
> undisturbed — `1-results.md` S0 P1) is asserted in the **8-B2 acceptance**, not here.

### L4. CRE origination report → ZIP_CONTROLLER.onReport (Branch a)
- Caller: `FORWARDER` (CRE DON-signed delivery, or simulated via test fixture)
- Via: CRE Forwarder
- Target.fn: `ZIP_CONTROLLER.onReport(metadata, report)` where `report = abi.encode(uint8 reportType=1, bytes payload)` and `payload = abi.encode(lienId, proofRef, equityMark, borrowLTV, liqLTV, drawAmount, cap)` (**`proofRef`** = Proof attestation commitment; **no `regionalHPI`**, §4.4). Precondition: the CRE only emitted this report because the **Proof of Lien + Proof of Insurance** gates passed off-chain (§8.5).
- Internal flow (asserted as a transcript):
  1. `_decodeMetadata` workflow-identity check passes.
  2. `_processReport` decodes `reportType == 1`.
  3. Precompute `LIEN_i = LIEN_FACTORY.computeAddress(lienId, ZIP_CONTROLLER)` (= `CREATE2(LIEN_FACTORY, keccak256(abi.encode(lienId)), keccak256(initCode))` with `initCode` embedding `ZIP_CONTROLLER`). Assert `LIEN_i` matches the address that's about to be deployed.
  4. `LIEN_FACTORY.create(lienId)` — called **by `ZIP_CONTROLLER`**, so the caller (`msg.sender = ZIP_CONTROLLER`) becomes the token's authority — deploys `LIEN_i` (a `LienCollateralToken`, totalSupply = `1e18` minted to controller, decimals = 18, mint/burn restricted to controller). The address binds to the caller, so only `ZIP_CONTROLLER`'s call yields this `LIEN_i`.
  4b. `VENUE.openLine(lienId, LIEN_i, 1e18)` returns `(lineRef = EVAULT_i, oracleKey = LIEN_i)` — mints the escrow collateral vault + USDC borrow vault + the per-line `ROUTER_i` (wired `COLLAT_i→LIEN_i→ZIP_ORACLE_REG`, frozen) + EE onboarding (see S9, subsumed here).
  5. `ZIP_CONTROLLER` calls `ZIP_ORACLE_REG.seedPrice(LIEN_i, equityMark)` (controller-gated, set-once `controller` wired at S6) → seeds `cache[LIEN_i] = (equityMark = Proof of Value − senior debt, block.timestamp)` — the atomic seed. The oracle key is `LIEN_i` (unchanged); `ROUTER_i` resolves `COLLAT_i → LIEN_i → ZIP_ORACLE_REG`. **No shared-router `govSetConfig`/`govSetFallbackOracle`** — the per-line `ROUTER_i` was wired + frozen inside `openLine` (§4.1/§4.7).
  6. `VENUE.setLineLimits(EVAULT_i, borrowLTV, liqLTV, cap)` (Euler adapter: `EVAULT_i.setLTV(COLLAT_i, …)` — registers the escrow vault as collateral).
  7. `VENUE.fund(line, drawAmount)` + `VENUE.draw(line, drawAmount, EREBOR)` on the line's fresh `LINE_BORROW_ACCOUNT` (the `LineAccount` prefix's sub-account 1, §4.4 — not a controller sub-account) (Euler adapter: `EVC.batch([enableController, enableCollateral, EVAULT_i.borrow]` on-behalf of `LINE_BORROW_ACCOUNT`; `EE_POOL.reallocate` is the `fund` step).
- Expected events: `LienCreated(lienId, LIEN_i)`; `RegistryPriceSeed(LIEN_i, equityMark)`; `LTVSet`; `Allocation`; `Borrow(EVAULT_i, drawAmount, EREBOR, onBehalfOf=LINE_BORROW_ACCOUNT)`. (No `OracleConfigSet` — no per-lien router config.)
- Post-state assertions:
  - `LIEN_i.totalSupply() == 1e18`; `LIEN_i.decimals() == 18`; `LIEN_i.balanceOf(EVAULT_i) > 0` (deposited as collateral).
  - `ROUTER_i.resolveOracle(1e18, COLLAT_i, USDC)` resolves to `(_, LIEN_i, USDC, ZIP_ORACLE_REG)`; `ROUTER_i.governor() == address(0)` (frozen).
  - `ZIP_ORACLE_REG.getQuote(1e18, LIEN_i, USDC) == equityMark` (exact for `inAmount == 1e18` — the scale convention pins `getQuote(1e18) == price`, §4.1; `≈` only for fractional/odd inputs via `fullMulDiv` floor).
  - `EVAULT_i.LTVBorrow(COLLAT_i) == borrowLTV` and `EVAULT_i.LTVLiquidation(COLLAT_i) == liqLTV` (collateral = the escrow vault, 1e4 scale).
  - `EVAULT_i.debtOf(LINE_BORROW_ACCOUNT) == drawAmount` (the line's fresh per-line borrow account, §4.4 — not a controller sub-account).
  - `USDC.balanceOf(EREBOR) == drawAmount` (USDC routed to the off-ramp).
  - **Hook passed:** transaction succeeded; no `CallerNotAuthorized`-style revert. (Cross-check: from a separate read, the gate clears for the line account via `EVC.isAccountOperatorAuthorized(LINE_BORROW_ACCOUNT, VENUE) == true` — the per-line `LineAccount` granted the `VENUE` borrow-driver the operator bit at origination, §4.4/§4.7.)
- Notes:
  - **`LIEN_i` address precomputation** must match the CREATE2 deployment. If they diverge, the batch's later steps reference the wrong address and the test fails *here*, which is the right place to catch it.
  - **No timelock-vs-atomic-seed conflict (per-line-router design, supersedes F4):** origination writes `cache[LIEN_i]` **directly** to the registry; the per-line `ROUTER_i` (minted + wired + frozen inside `openLine`) routes `(COLLAT_i → LIEN_i, USDC)` there, so there is **no shared-router `govSetConfig`** and nothing to schedule through a timelock at origination (§4.1/§4.7). Per-line routers are immutable (frozen), strictly stronger than the old timelock veto.

### L5. Erebor off-ramp (off-chain, mocked)
- Off-chain: the test scribe records that `drawAmount` USDC was wired to the originator (no on-chain effect beyond `USDC.balanceOf(EREBOR)` already asserted in L4).

### L6. Interest accrual
- Action: advance block time by 1 year (`vm.warp(block.timestamp + 365 days)` in a fork test; impractical on real mainnet, so the fork-warp is the method).
- Trigger any view function that touches `EE_POOL.totalAssets()` (e.g. `_accrueInterest`), or call `EE_POOL.accrueInterest()` if exposed. Perf-fee mint mechanic from §5 fires: feeShares minted to the **protocol-side fee holder** (the lending yield is the **protocol's**, §5/§17 — recipient pending 8-S2b), **not** szipUSD; the junior's pay is the xALPHA subsidy + HYDX vamp (`1-results.md` S0 P3–P5).
- Post-state assertions:
  - `EVAULT_i.debtOf(LINE_BORROW_ACCOUNT) ≈ drawAmount × (1 + APR × Δt)` (≈ 108,000e6 for 8% APR / 1 year).
  - `EE_POOL.totalAssets()` is approximately `1,200,000e6 + interest_share_of_principal` (only the lien earned interest in this test; the idle USDC didn't).
  - `EE_POOL.balanceOf(<protocol-side fee holder, wired at 8-Bw>)` > pre-accrual balance (fee-shares minted protocol-side; was `SZIPUSD` under the deleted model).
  - **I1 sanity (`1-results.md` I1):** `NAV_s/Z ≥ 1` — the accrued interest lifts the **protocol's surplus σ**, so `R` rises above 1.0 (it does **not** accrue to a junior `ν=J×p` term, which no longer exists).

### L7. Permissionless repay
- Caller: any EOA (e.g. `LP_B` or a test relayer) — the point is *anyone* can call `repay`.
- Direct: `USDC.approve(EVAULT_i, debt)`; `EVAULT_i.repay(debt, LINE_BORROW_ACCOUNT)`.
- Expected events: `Repay(EVAULT_i, caller, LINE_BORROW_ACCOUNT, debt)`. No hook events (OP_REPAY is ungated).
- Post:
  - `EVAULT_i.debtOf(LINE_BORROW_ACCOUNT) == 0` OK
  - Hook *not* invoked (assert via event absence or trace).
  - `EE_POOL.totalAssets()` reflects the repaid principal + interest re-entering the pool.

### L8. CRE close report → ZIP_CONTROLLER.onReport (Branch c)
- Caller: `FORWARDER`
- Target.fn: `ZIP_CONTROLLER.onReport(metadata, report)` with `reportType = 4` (Close), `payload = abi.encode(lienId)`.
- Internal: branch (c) checks `EVAULT_i.debtOf(LINE_BORROW_ACCOUNT) == 0` (via `VENUE.observeDebt`), then `VENUE.closeLine` reclaims the lien (operator-routed redeem) and `LIEN_i.burn(1e18)`, emits `LienReleased(lienId)`.
- Expected events: `LienReleased(lienId)`; `Transfer(LIEN_i: holder → 0, 1e18)`.
- Post:
  - `LIEN_i.totalSupply() == 0`.
  - Lien lifecycle state = `[closed]` (per §10).

### L9. Senior redeem (zipUSD via queue)
- Caller: `LP_A`
- Direct: `ZIPUSD.approve(REDEMPTION_QUEUE, 500_000e6)`; `REDEMPTION_QUEUE.requestRedeem(500_000e6, LP_A, LP_A)` (7540 surface).
- Expected events: `RequestRedeem(LP_A, 500_000e6)`; `Transfer(zipUSD: LP_A → REDEMPTION_QUEUE, 500_000e6)` (escrowed).
- Post:
  - `ZIPUSD.balanceOf(LP_A)` decreased by 500_000e6 (escrowed).
  - `REDEMPTION_QUEUE.pendingRedeem(LP_A) == 500_000e6` (or equivalent 7540 view).
  - Repeat the request for `LP_B`, say 100_000e6, so the epoch has two requesters with different sizes (tests pro-rata).

### L10. Epoch settle
- Action: advance time 30 days; controller calls `settleEpoch()` (via CRE cron, simulated).
- Caller: `ZIP_CONTROLLER` (privileged settle path).
- Internal: read `freeable = min(pendingRedeem_total, EE_POOL.maxWithdraw(protocol))`; compute `redeemable_i = queued_i × freeable / total_queued`; burn filled zipUSD; withdraw USDC from EE_POOL; mark `claimable[i]`.
- Expected events: `EpochSettled(epochId, freeable, total_queued)`; per-requester `Claimable(LP_A, X)`, `Claimable(LP_B, Y)`.
- Post-state assertions:
  - `claimable[LP_A] + claimable[LP_B] == freeable USDC at settlement`.
  - `LP_A_claimable / LP_B_claimable == 5 / 1` (pro-rata; LP_A requested 500k vs LP_B's 100k).
  - Burned zipUSD = sum of filled portions; carry-forward = remainder if liquidity short.
  - Total `ZIPUSD.totalSupply()` decreased by exactly the filled portion (not the requested portion).

### L11. Claim
- Caller: `LP_A` (then `LP_B`)
- Direct: `REDEMPTION_QUEUE.withdraw(claimable[LP_A], LP_A, LP_A)`.
- Post:
  - `USDC.balanceOf(LP_A)` increased by `claimable[LP_A]`.
  - `claimable[LP_A] == 0` after.

### L12. Junior exit (Baal ragequit)
> **EXCISED (2026-06-06, 8-S3) — convert-on-stake cooldown + `unstake` deleted.** The old step had JR
> `SZIPUSD.startCooldown(shares)` then (after 30 days) `SZIPUSD.unstake(shares, JR)` — gated by
> `availableWithdrawLimit` (cooldown + floor + freeze), returning EE pool shares to the module and minting
> zipUSD to JR at the realized share price. **The Baal junior has no cooldown-`unstake` and no EE-share
> accounting.** JR exits by **Baal `ragequit`** — receives `(JR Loot / total Loot) × basket` **in-kind**
> (zipUSD + xALPHA + LP), value-preserving (`1-results.md` I3), gated by the ~30-day **lock** and any active
> **freeze** (the 8-B3 lock/freeze shaman). `Z` and `NAV_s` are unchanged by the exit (the basket's zipUSD
> moves Safe → JR, still backed); JR then exits zipUSD → USDC via the epoch queue (§6.1). The ragequit + the
> lock/freeze gate acceptance is authored at **8-B1 (ragequit) / 8-B3 (lock+freeze)**, not here.

**Phase L complete when:** the senior steps (L1–L2, L4–L11) produce the expected events and post-state
assertions hold; final lien is `[closed]`; the senior epoch settled. **The junior deposit (L3) and exit (L12)
are authored with the 8-B Baal tickets** (their acceptance lives there, not in this M1 script).

---

## 5. Phase N — negative tests (every one must revert with the indicated reason)

### N1. Permissionless borrow attempt
- Caller: `EOA_X` (an unrelated EOA)
- Direct: `EVAULT_i.borrow(1, EOA_X)` (on its own account).
- Expected revert: `HookReverted` from the gating hook — `EVC.isAccountOperatorAuthorized(EOA_X, VENUE) == false` (`EOA_X` never authorized the `VENUE` borrow-driver as its operator).

### N1b. External liquidation attempt on an interest-underwater line (defensive gate)
- Setup: warp far enough that `LINE_BORROW_ACCOUNT` (the line's fresh per-line borrow account, §4.4) has drifted past `liqLTV` from interest accrual (no default — purely time). There is **no on-chain economic liquidation** for a home lien (§4.4e); the `OP_LIQUIDATE` gate exists only to block external seizure (§4.3).
- Caller: `EOA_X` (any non-controller party).
- Direct: `EVAULT_i.liquidate(violator=LINE_BORROW_ACCOUNT, collateral=LIEN_i, …)`.
- Expected revert: `HookReverted` from the gating hook — `EVC.isAccountOperatorAuthorized(EOA_X, VENUE) == false` (the appended on-behalf account is `EOA_X`, which never authorized the `VENUE` borrow-driver as operator). (Confirms the line cannot be seized; resolution stays off-chain → permissionless `repay`.)

### N2. Bad workflow identity in CRE report
- Caller: `FORWARDER` (correct sender)
- Direct: `ZIP_CONTROLLER.onReport(metadata_with_wrong_workflowId, report)`.
- Expected revert: `InvalidWorkflowId(received, expected)` (from `ReceiverTemplate`'s identity check).

### N2b. Wrong forwarder
- Caller: some non-Forwarder EOA.
- Direct: `ZIP_CONTROLLER.onReport(metadata, report)`.
- Expected revert: `InvalidSender(msg.sender, FORWARDER)`.

### N3 + N4. Junior exit-gate negative tests (Baal ragequit-gate)
> **EXCISED (2026-06-06, 8-S3) — convert-on-stake exit-gate tests deleted.** The old N3 (`unstake` without
> `startCooldown` → `ExceedsWithdrawLimit`) and N4 (`unstake` below the subordination floor → `ExceedsWithdrawLimit`)
> tested the `availableWithdrawLimit` gate of the deleted cooldown-`unstake`. **The Baal equivalents are
> ragequit-gate reverts:** a `ragequit` inside the ~30-day **lock** must revert (lock-shaman); a `ragequit` of
> the at-risk **frozen** slice during a Duration-Bond freeze must revert (freeze-shaman, `lockedFraction =
> atRiskAmount/basketNAV`, `1-results.md` I4); a `ragequit` below the governed **withdrawal floor** must revert.
> These negative tests are authored with the **8-B3 lock/freeze shaman** ticket, not here.

### N5. Re-wire a frozen per-line router
- Caller: anyone (incl. `ZIP_CONTROLLER` / `VENUE`)
- Direct: on an open line's `ROUTER_i`, `govSetConfig(LIEN_i, USDC, ATTACKER_ORACLE)` or
  `govSetResolvedVault(...)` or `transferGovernance(...)`.
- Expected revert: `Governance_OnlyGovernor` (or the router's unauthorized-governor error) — `ROUTER_i` was
  `transferGovernance(address(0))`-frozen inside `openLine` (§4.1/§4.7), so **no** caller can re-point a line's
  price wiring. (Replaces the old shared-router-vs-timelock test: there is no shared router; per-line
  immutability is stronger than a timelock veto.)

### N6. Over-LTV origination
- Caller: `FORWARDER` (origination report)
- Report payload: `drawAmount > borrowLTV × equityMark`.
- Expected: the `EVC.batch` reverts in the borrow step, surfaced by EVK's account-status check (`E_LTVBorrow` or equivalent).

### N7. Forwarder mutation attempt
- Caller: anyone (the old owner is renounced; nobody has `Ownable` authority)
- Direct: `ZIP_CONTROLLER.setForwarderAddress(any)`.
- Expected revert: `OwnableUnauthorizedAccount(caller)` — the base `setForwarderAddress` is non-virtual and
  `onlyOwner`; ownership was renounced at S11 (owner == `address(0)`), so every owner-gated setter reverts.
  (Same for `ZIP_ORACLE_REG.setForwarderAddress`/`setController`.) §4.1/§4.4.

### N8. Re-renounce / re-grant timelock admin
- Caller: anyone
- Direct: any attempt to grant `TIMELOCK_ADMIN_ROLE` to a new address.
- Expected revert: admin role is renounced; no caller has authority.

---

## 6. Acceptance criteria

You have completed `2.md` when:
- **Phase S** completes; every assertion holds; `Ownable` ownership is renounced where stated.
- **Phase L** runs end-to-end with every post-state assertion OK.
- **Phase N (N1–N8)** each revert with the expected error (capture the revert data / selector).
- Final ledger reconciles: total USDC in (LP deposits) ≈ total USDC out (LP redeems + Erebor draw [returned
  via repay]) modulo accrued interest and rounding. (The junior leg is in-kind ragequit, not a USDC exit — its
  reconciliation lives in the 8-B acceptance.)
- A consolidated run log is posted at `2-results.md`: one row per step (S, L, N) with caller / tx hash /
  events / assertion result. Any failure is captured with the on-chain revert reason + the spec § it
  contradicts.

If a step *cannot run* because the spec is silent on a mechanism (e.g. exactly how the venue adapter onboards
a per-line market inside `openLine`, §4.7), that is a finding: log it, do not invent the mechanism. The spec
must be tightened first.

When `2.md` passes, M1 is accepted. (The authority & gating wiring was validated pre-build in
`3-results.md`, and the money model in `1-results.md`; `2.md` is the on-chain acceptance harness — the
Foundry tests in `contracts/test/`.)

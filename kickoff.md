# kickoff.md — paste this to start each authoring window

You're authoring zipcode-euler build tickets, **one item per context window**: complete exactly ONE item,
conclude it on disk, then STOP for review. Do not mass-produce.

1. **Read `nextsteps.md`** — your full orientation: the reading order, the harness loop, the keep-the-build
   doctrine, the conclude/report steps, and the locked `§17` decisions you must not reopen.
2. **Read `tickets/PROGRESS.md`** — the item marked **`NEXT`** is your task. Check "Open cross-ticket
   obligations" for any rows owed by it (the ticket MUST discharge each; mark `DISCHARGED` at Conclude).
3. **Run the harness** (`audit/adversarial-spec/README.md`) → **build it for real + KEEP it** → file the
   ticket in `tickets/<team>/` → **conclude** (update `PROGRESS.md`, commit the code, write
   `reports/<ITEM>-report.md`) → **STOP** for the superintendent.

Everything else — the method detail, the rules, the repo map — lives in `nextsteps.md` + the harness. This
note is just the trigger.

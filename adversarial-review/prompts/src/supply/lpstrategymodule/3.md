# Mission 3 — The swap-safety adversary (sandwich, dangling approval, swallowed failure)

MISSION: 3 — slippage floors (I-3), approval hygiene + atomic rollback (I-2), bubbled false-return (I-4)

## Who you are
You attack the deposit/withdraw safety mechanics: the claims that a direct ICHI deposit can't be sandwiched past
its slippage floor, that no approval survives an add/stake (or a failed one), and that a Safe-swallowed
vault/gauge revert always hard-reverts. Your goal: ONE path where a deposit mints below `minShares` without
reverting, where an allowance dangles, or where a failed deposit/stake is reported as success.

## The claims you are attacking
- **I-3 (slippage floors).** `addLiquidity` requires `minShares != 0` (`:221`, `ZeroMinShares`) and reverts
  `Slippage` if `shares < minShares` (`:243`). `removeLiquidity` reverts `Slippage` if `amount0 < minAmount0 ||
  amount1 < minAmount1` (`:272`). **Find a path where a deposit undershoots the floor without reverting** — e.g.
  the `shares` decode (`:233`) reading a stale/wrong value, or the floor compare (`:243`) running on the wrong
  variable. Note: the deposit is on a DIRECT ICHI vault (no router), so `minShares` is the ONLY sandwich
  protection — its bypass is a real loss.
- **I-2 (no standing approval + atomic rollback).** `addLiquidity` approves the non-zero legs (`:224-229`),
  deposits (`:232`), then resets the legs to 0 (`:236-241`). `stake` approves → deposit → reset (`:280-282`).
  **Find a path where a reset does NOT run** — a revert between approve and reset leaving a non-zero allowance. If
  the deposit (`:232`) reverts, the whole tx rolls back, unwinding the approves — confirm no partial commit. Note
  the reset (`:236-241`) is AFTER the deposit but BEFORE the slippage check (`:243`) — so a `Slippage` revert
  (`:243`) ALSO rolls back the resets and the deposit (whole tx). Confirm allowance ends at 0 in every outcome.
- **I-4 (exec false-return hard-reverts with bubbled data).** `_exec` (`:198-207`) reverts on `!ok`, bubbling
  `ret` (`:202-204`) or `ExecFailed` (`:201`). **Find a path where a failed vault/gauge call returns `(false, …)`
  yet the module proceeds** — e.g. the `shares`/`(amount0, amount1)` decode (`:233`, `:271`) reading a plausible
  value from a failed call. The X-Ray notes the disallowed-side fail-close (real vault `allowToken1 == false`).

## Specific attacks to run
1. **The slippage-after-reset ordering.** In `addLiquidity`, the order is approve (`:224`) → deposit (`:232`) →
   decode shares (`:233`) → reset approvals (`:236`) → `if (shares < minShares) revert Slippage()` (`:243`).
   Confirm that a `Slippage` revert at `:243` rolls back EVERYTHING (the deposit too) — so a sandwiched deposit
   that undershoots leaves the basket unchanged, not holding bad LP. (Whole-tx revert; no state. Confirm.)
2. **Single-sided vs balanced legs.** `addLiquidity` skips the zero leg's approve/reset (`:224`, `:227`, `:236`,
   `:239`). Confirm a single-sided deposit (one leg 0) approves + resets ONLY the non-zero leg, and that the
   other leg never gets a dangling approval. Confirm `deposit0 == 0 && deposit1 == 0` reverts `ZeroAmount`
   (`:220`).
3. **The decode trust.** `shares = abi.decode(ret, (uint256))` (`:233`); `(amount0, amount1) = abi.decode(ret,
   (uint256, uint256))` (`:271`). Confirm a short/empty `ret` can't decode to a misleading value — `_exec`'s
   length check (`:201`) catches empty; a successful deposit/withdraw returns the right-width data.
4. **Disallowed-side fail-close.** The ICHI vault's `allowToken0`/`allowToken1` gate leg legality. A deposit on a
   disallowed side reverts inside the vault → bubbled via `_exec`. Confirm the module doesn't pre-assume both
   legs allowed and that a disallowed-side revert surfaces (the X-Ray's `test_fork_disallowed_side_reverts`).
5. **Token approve-race quirk.** The reset approves 0. For a USDT-style token (revert on approve-nonzero-when-
   nonzero), confirm the reset-to-0 between deposits avoids the race (next deposit goes 0→amount). The wired legs
   (zipUSD/xALPHA/WETH/USDC) — do any have the quirk?

## What is already proven (don't re-report as new)
`test_addLiquidity_slippage_floor`, `test_zero_minShares_reverts`, `test_removeLiquidity_slippage_floor`,
`test_fork_slippage_floor_snapshot_guarded` (probe-then-floor+1 → `Slippage`), `test_exec_discipline_*` +
`test_atomicity_addLiquidity_single_deposit_fail_rolls_back` / `_both_legs_resets_both` /
`_stake_deposit_fail_rolls_back`, `test_exec_bubbles_custom_error` / `_no_data_falls_back_to_ExecFailed`,
`test_disallowed_side_bubbles_on_mock` / `test_fork_disallowed_side_reverts`. Confirming the floors + hygiene +
bubble hold goes in your Summary. A finding must be a concrete sandwich / dangling-approval / swallowed-failure
path the slippage + atomicity + bubble tests do NOT cover.

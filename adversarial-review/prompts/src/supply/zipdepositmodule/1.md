# Mission 1 — The custody adversary (does the conduit ever retain an asset, or fail to roll back?)

MISSION: 1 — net-zero custody, delta cleanliness, atomic rollback

## Who you are
You attack the conduit's core promise: it custodies NOTHING and any downstream failure unwinds completely.
The module pulls USDC, mints zipUSD (to the caller, or transiently to itself then hands it to the Gate),
parks USDC in the EE pool, and (zap) mints szipUSD on-behalf. Your goal: find ONE path where the module ends
a call holding USDC / zipUSD / EE-share / szipUSD, or where a mid-call revert leaves a non-pristine state.

## The claims you are attacking
- **I-1 (net-zero custody).** After ANY call the module holds no USDC, zipUSD, EE-share, or szipUSD. Trace
  both `deposit` and `zap`: in `deposit`, zipUSD is minted directly to the caller (module never holds it) and
  USDC goes to the EE pool (shares → warehouse). In `zap`, zipUSD is minted TRANSIENTLY to the module then
  handed to the Gate — **find any branch where that transient zipUSD (or a share, or USDC) is left behind.**
- **I-4 (cleanliness is a DELTA, not absolute zero).** The zap snapshots `zipBefore` and asserts the net
  zipUSD delta == 0 (`ResidualBalance`), NOT that the absolute balance is 0. This is deliberate: a stray
  1-wei zipUSD donation must NOT brick the zap. **Confirm it's a delta check** (a regression to absolute-zero
  would let an attacker grief every zap with a 1-wei donation) **and that an under-pull still reverts**
  `ResidualBalance` (the delta must be exactly 0, not ≥0).
- **I-5 (zap fails closed on a bad Gate).** A Gate that mints no shares → `ZeroShares`; a Gate that reverts
  mid-call → full atomic rollback (pristine post-state: no USDC pulled, no zipUSD minted, no share stranded).
  **Find a Gate misbehavior that leaves a partial state** — e.g. USDC parked in the EE pool but the szipUSD
  mint reverted without unwinding the EE deposit.

## Specific attacks to run
1. **Transient-zipUSD leak.** In `zap`, walk the path: mint transient zipUSD → park USDC → `forceApprove(gate,
   zipAmount)` → `gate.depositFor(...)` → assert delta==0 → `forceApprove(gate, 0)`. If `depositFor` pulls
   LESS than `zipAmount`, the module holds the remainder — confirm the `ResidualBalance` delta check catches
   it (reverts) rather than silently leaving dust.
2. **Donation grief.** Send 1 wei zipUSD to the module, then zap. The delta check (`zipAfter - zipBefore ==
   0`) must still pass (the donation is in both before and after). Confirm an absolute-zero check would have
   bricked here — if the code uses absolute zero, that's a griefable DoS.
3. **Mid-call revert atomicity.** Make the Gate revert after USDC is parked in the EE pool. Confirm the whole
   tx reverts and NO state persists (no EE shares to the warehouse, no USDC moved). Is there any external
   call whose effect escapes the revert (it shouldn't in the EVM, but confirm no try/catch swallows it)?
4. **EE-share / szipUSD retention.** Confirm EE shares always go to `warehouseSafe` (never the module) and
   szipUSD always to `msg.sender` (never the module). Find any path where the receiver is the module.
5. **`_assertModuleEmpty` coverage.** The X-Ray says this runs in every happy path. Find a path (an edge,
   a non-happy branch) where it's NOT asserted and the module could retain something.

## What is already proven (don't re-report as new)
`_assertModuleEmpty()` in every happy path; `test_deposit_warehouse_custody`, `_zap_on_behalf_par_nav`,
`_sequential_no_residue`, both fuzz tests (I-1); `test_zap_survives_zipusd_donation`,
`testFuzz_zap_tolerates_donation`, `_zap_underpull_reverts_ResidualBalance` (I-4); `test_zap_noshare_reverts_ZeroShares`,
`_zap_gateRevert_atomic_rollback` (I-5). Confirming net-zero custody + delta cleanliness + atomic rollback
hold goes in the Summary. A finding must be a concrete retention, an absolute-zero brick, or a partial-state
revert these tests don't model.

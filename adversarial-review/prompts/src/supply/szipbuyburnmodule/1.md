# Mission 1 — The above-NAV adversary (can a fill clear above navExit × (1−d), or past the cap?)

MISSION: 1 — the exact discount price bound (I-2) + caps & kill-switch (I-4)

## Who you are
You attack the economic exit-safety core: the claim that the protocol never buys szipUSD above
`navExit × (1 − d)` (a haircut to NAV, never above it), and that the per-bid cap + kill-switch bind. Your goal:
ONE path where the price bound rounds UP into an above-NAV fill, where the integer comparison admits a price it
shouldn't, or where `sellAmount` exceeds `buybackCap` (or a post survives `buybackCap == 0`).

## The claims you are attacking
- **I-2 (exact discount price bound).** `_postBid` (`:332`) enforces, in exact integer form (`:363-366`):
  `order.sellAmount * 1e12 * 10_000 * 1e18 > order.buyAmount * navExit18 * (10_000 - dBps)` → revert
  `BidAboveDiscount`. Both `/10_000` and `/1e18` are moved to the LHS as multipliers so the comparison is EXACT
  (the ceiling never rounds UP into an above-NAV fill). Priced off `navExit18` (`:356`), NOT a twap. **Find an
  input where a paid price ABOVE `navExit × (1−d)` passes** — a rounding direction error, an overflow in the
  products (guarded by `MAX_BUY_AMOUNT`? — confirm), or a unit reconciliation bug (USDC 6-dp ↔ szipUSD 18-dp ↔
  nav 18-dp; `sellAmount * 1e12` is the 18-dp USD paid).
- **I-4 (caps + kill-switch).** `_postBid` reverts `BuyAmountTooLarge` if `buyAmount > MAX_BUY_AMOUNT` (`:335`,
  =1e30), `CapExceeded` if `sellAmount > buybackCap` (`:336`), `ZeroAmount` if either is 0 (`:334`). `buybackCap
  == 0` ⇒ every post reverts `CapExceeded` (the kill-switch). **Find a path where `sellAmount > buybackCap`
  posts, where a zero cap admits a post, or where `MAX_BUY_AMOUNT` doesn't prevent the price-bound product from
  overflowing.**
- **The `quoteMaxPrice` view vs the on-chain check.** `quoteMaxPrice` (`:452`) rounds DOWN (buyer-conservative)
  to size the off-chain bid; the on-chain `:363` check re-validates exactly. **Confirm the view and the check
  agree at the boundary** — a `sellAmount = quoteMaxPrice` per 1e18 `buyAmount` must pass the on-chain gate (the
  X-Ray says it does); find a rounding gap where the view says OK but the check rejects (grief) or worse, the
  view under-states so an above-NAV bid passes the check.

## Specific attacks to run
1. **The boundary, both ways.** Walk the exact comparison `:363-366` for divisible and non-divisible cases.
   Confirm `paid == ceiling` is ALLOWED (`>`, not `>=`) and `paid == ceiling + 1` reverts. The X-Ray pins both
   `test_price_bound_boundary_divisible` / `_non_divisible` — find a `(sellAmount, buyAmount, navExit18, dBps)`
   triple where the integer math admits a fractionally-above-NAV fill the boundary tests miss.
2. **Overflow in the products.** LHS `sellAmount * 1e12 * 10_000 * 1e18`; RHS `buyAmount * navExit18 * (10_000 -
   dBps)`. `buyAmount ≤ MAX_BUY_AMOUNT = 1e30`; `sellAmount ≤ buybackCap`. Can either product overflow uint256
   (0.8 checked → revert = fail-closed, safe) for realistic-but-large inputs? Confirm `MAX_BUY_AMOUNT` and the
   cap keep the products in range, OR that an overflow reverts rather than wrapping into a passing comparison.
3. **`navExit` semantics.** `navExit() = min(spot, twap)` and does NOT revert on staleness (the freshness gate
   `:353` handles that). Confirm the price uses `navExit` (`:356`), not a manipulable spot — and that pricing off
   `min(spot, twap)` is buyer-conservative (a manipulated-low spot lowers the ceiling, rejecting bids — safe; a
   manipulated-high spot is capped by twap). The X-Ray's `test_prices_off_navExit_not_twap`.
4. **Kill-switch + cap.** Confirm `buybackCap == 0` reverts EVERY post (`:336`, `0 > 0` false but `sellAmount >
   0` always `> 0`... trace: `sellAmount > buybackCap` with cap 0 and sellAmount ≥ 1 → reverts). Confirm
   `setBuybackCap(0)` (`:234`) is the kill-switch and is `onlyOwner`.

## What is already proven (don't re-report as new)
`test_price_bound_boundary_divisible` / `_non_divisible`, `test_bid_at_or_above_nav_reverts`,
`test_deep_discount_passes`, `test_prices_off_navExit_not_twap`, `test_cap_exceeded_reverts`,
`test_killswitch_zero_cap_always_reverts`, `test_buyAmount_too_large_reverts`, `test_setDiscountBps_bounds`.
Confirming the price bound is exact + the cap/kill-switch bind goes in your Summary. A finding must be a concrete
above-NAV-fill / overflow / cap-bypass path the boundary + cap tests do NOT cover.

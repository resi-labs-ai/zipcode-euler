# Mission 1 — The borrower-of-record adversary (is the `^ 1` sub-account correct + isolated?)

MISSION: 1 — LineAccount is the prefix owner (I-1), `borrowAccount == lineAccount ^ 1` (I-2), code-free (I-3),
adapter is the granted operator (I-4), per-line isolation (I-5/I-6), CREATE2 collision (I-7)

## Who you are
You attack the one consequential property of an 8-nSLOC constructor-only contract: the `^ 1` sub-account choice
and the operator grant. The borrow account must be BOTH prefix-sharing (so the owner-self grant works) AND
code-free (so the EVC `:787` guard passes); the adapter must be the granted operator; and each line's grant must
NOT reach another line's account. Your goal: ONE path where the XOR is wrong (deploy reverts or the borrow path
is unauthorized), the grant lands on the wrong account/operator, or two lines cross-authorize. (The contract is
inert after birth — there is no post-construction surface; soundness is the expected result.)

## The claims you are attacking
- **I-2/I-3 (the `^ 1` sub-account — the load-bearing line).** `borrowAccount = address(uint160(address(this)) ^
  1)` (`:21`). **Confirm BOTH:** (a) `^ 1` keeps the high 19 bytes (the prefix) identical to `address(this)` — it
  only flips the low bit of the last byte, so `borrowAccount` is sub-account 1 of THIS contract's prefix (so
  `setAccountOperator`'s owner-self path can authenticate); (b) `borrowAccount` is CODE-FREE — it is a plain
  account address, NOT this coded contract's address, so the EVC's non-owner-must-be-code-free guard
  (`EthereumVaultConnector.sol:787`) does NOT trip. Find any way the XOR breaks either property (e.g. if the
  contract's own address ended in an odd byte such that `^ 1` collided with a coded address — confirm it can't,
  the sub-account is a distinct address with no deployed code).
- **I-1/I-4 (owner-self grant + the operator).** `IEVC(evc_).setAccountOperator(borrowAccount, operator_, true)`
  (`:25`) on the owner-self path — `authenticateCaller(borrowAccount, allowOperator:true)` finds the shared
  prefix, registers THIS contract as the prefix OWNER (I-1), then sets the operator bit for `operator_` over
  sub-account 1 (I-4). **Confirm** `operator_` is the ADAPTER (the adapter passes `operator_ = address(this)`
  from `openLine:326`), so the adapter becomes the authorized `EVC.call`/`EVC.batch` `msg.sender` on the borrow
  path. Find a path where the owner registration fails, or the operator bit is set for the wrong identity.
- **I-5/I-6 (per-line isolation).** Distinct CREATE2 prefixes per `lienId` (salt = `lienId`) mean one line's
  adapter grant CANNOT touch another line's account. **Confirm** two lines get distinct prefixes (distinct
  `address(this)` → distinct `^ 1` borrow accounts) and a FOREIGN account (not minted here) did NOT grant the
  adapter (so the gating hook rejects it at draw). Find a salt/prefix collision that could cross-authorize.
- **I-7 (CREATE2 collision).** Reusing a `lienId` reverts the CREATE2 deploy (one cluster per lien). Confirm a
  second deploy with the same salt reverts (surfaced by `openLine`'s double-open).

## Specific attacks to run
1. **The XOR arithmetic.** `address(uint160(address(this)) ^ 1)` (`:21`) — confirm it flips ONLY bit 0, so the
   result shares the 19-byte EVC owner-prefix (the EVC defines a prefix as 160−8 = the high 19 bytes; sub-accounts
   0..255 share it). Confirm `^ 1` yields sub-account 1 (not the contract's own sub-account 0), so granting the
   operator over it doesn't grant over the contract's own account. A `^ 0` (no-op) or a `+ 1` (could carry across
   the prefix boundary at a 0xFF last byte) would be the footgun — confirm `^ 1` is used, not `+ 1`.
2. **Code-freeness vs the EVC `:787` guard.** Read `EthereumVaultConnector.sol` around `:787` — the
   non-owner-must-be-code-free guard. Confirm `setAccountOperator(borrowAccount, ..)` on the owner-self path is
   NOT subject to it (the caller IS the prefix owner via the shared prefix), OR that the code-free `^ 1` account
   satisfies it regardless. The X-Ray's I-3 asserts `borrowAccount.code.length == 0` — confirm the chosen
   sub-account genuinely has no code (it's a never-deployed plain address).
3. **The operator argument.** Confirm the ctor grants the operator to the `operator_` ARG (`:25`), and that
   `openLine` (`:326`) passes the ADAPTER as `operator_`. If it granted to the wrong address (e.g. the EVC, the
   controller, `address(this)` of the LineAccount itself), the adapter's `draw`/`closeLine` `EVC.call`/`batch`
   would be UNAUTHORIZED. Trace the adapter's use of `borrowAccount` (`draw:457`/`:483`, `closeLine:534-540`) and
   confirm the granted operator matches the `EVC.batch`/`call` `msg.sender` (the adapter).
4. **Isolation by distinct prefix.** Two `openLine`s with distinct `lienId`s → distinct CREATE2 addresses →
   distinct prefixes → distinct `^ 1` borrow accounts. Confirm line A's adapter-operator grant cannot authorize
   an action on line B's borrow account (different prefix, the EVC keys authorization per-prefix). Find any shared
   state or collision that breaks this.
5. **Inertness post-construction.** Confirm there is NO function, fallback, receive, storage write, or
   self-destruct — nothing callable after the ctor. The contract holds no value and has no code-reachable state,
   so the graveyard model (never reclaimed at close) is benign — do NOT report it as a leak.

## What is already proven (don't re-report as new)
`test_LineAccount_Mechanics` (pins I-1/I-2/I-3/I-4 together: owner == lineAccount, `borrowAccount == ^ 1`,
code-free, adapter is operator), `test_TwoLine_DistinctPrefix_BothDraw_Isolation` (I-6: distinct prefixes, both
draw), `test_ForeignAccount_HookRejects` (I-5: a non-line account is rejected), `test_DoubleOpenLine_SameLienId_Reverts`
(I-7: CREATE2 salt collision), and the whole `EulerVenueAdapter.t.sol` fork suite (every `openLine` mints a real
LineAccount). Confirming the `^ 1` is correct (prefix-sharing + code-free), the adapter is the operator, and
isolation holds goes in your Summary. A finding must be a concrete XOR/grant/isolation break the suite does NOT
cover — NOT a bug in the vendored EVC (out of scope) and NOT the ratified graveyard model.

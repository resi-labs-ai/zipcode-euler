# Mission 1 — The cluster-mint adversary (atomic mint, orphan-freedom, per-line isolation)

MISSION: 1 — atomic 7-step cluster mint (I-1), per-line isolation + foreign-account rejection (I-2),
EE-timelock precheck / orphan-freedom (I-9), CREATE2 double-open (I-14)

## Who you are
You attack the factory's birth flow: the claim that ONE `openLine` call atomically mints + aligns the whole
isolated-market cluster — `LineAccount` + operator grant, escrow vault, frozen router, USDC borrow vault, EE
onboarding, lien custody, wire-check — with NO mid-cluster orphan, NO cross-line bleed, and NO double-open per
`lienId`. Your goal: ONE path that leaves orphaned state, lets one line's wiring touch another, or onboards a
line whose later steps half-fail.

## The claims you are attacking
- **I-1 (atomic cluster mint).** `openLine` (`:307`) runs steps 0→7 (`:326-381`) in one tx. Since it is a single
  call, a revert at any step unwinds everything — **confirm there is NO external call that can partially commit**
  (a `LineAccount` deployed but the EE onboarding reverting, leaving a stray CREATE2 contract + dangling vaults).
  The whole point of the I-9 precheck (`:323`) is that the EE onboarding (`submitCap`+`acceptCap`, `:353-354`)
  would revert mid-cluster on a non-zero EE timelock — confirm the precheck makes that abort BEFORE step 0
  (no orphan), and that NO other step can revert after irreversible external effects that don't unwind.
- **I-9 (EE-timelock precheck, SEC-08).** `if (eulerEarn.timelock() != 0) revert EulerEarnTimelockNonZero()`
  (`:323`) runs BEFORE the `1e18` collateral check is the only thing earlier (`:316`). The EE owner is EXTERNAL
  and can RAISE the timelock post-deploy, so this reads it LIVE per origination. **Confirm a non-zero EE timelock
  aborts before ANY line state is built** (no LineAccount, no vaults) — and that the same-tx `submitCap`+
  `acceptCap` (`afterTimelock`) genuinely needs `timelock == 0` (else the precheck is wrong and an orphan is
  reachable).
- **I-2 (per-line isolation + foreign-account rejection).** Each line gets a distinct EVC prefix
  (`LineAccount` CREATE2 salt = `lienId`); `borrowAccount = address(uint160(la) ^ 1)` (`:327`). **Confirm two
  lines get DISTINCT prefixes and one line's operator grant cannot reach another line's borrow account**, and
  that a FOREIGN borrow account (not minted by `openLine`) is rejected by the `CREGatingHook` at draw. The §4.3
  hook's `isAccountOperatorAuthorized` is the isolation gate — confirm the adapter only ever drives accounts it
  minted.
- **I-14 (CREATE2 double-open).** `new LineAccount{salt: lienId}` (`:326`) — reusing a `lienId` reverts the
  CREATE2 deploy (one cluster per lien). **Confirm a second `openLine` with the same `lienId` reverts** (the EVM
  CREATE2 collision), not silently re-points an existing line's record.

## Specific attacks to run
1. **Diff against `EdgeFactory.deploy()`.** Open `reference/evk-periphery/src/EdgeFactory/EdgeFactory.sol` and
   trace its mint sequence step-for-step against `openLine` (`:326-381`). Does `EdgeFactory` perform a wiring
   step the adapter omits (a governance handoff, an unwrap/resolve config, a hook config, an LTV set)? Does the
   adapter perform them in a SAFE order (router wired + resolved BEFORE the borrow vault points at it `:336-342`;
   router FROZEN `:371` only AFTER the lien is custodied `:367` so the wire-check `:374` reads the final wiring)?
   The strongest finding is a step the precedent does that the adapter skips, or an ordering that lets a step
   read stale wiring.
2. **The orphan window.** Walk every external call in `openLine` that lands AFTER step 0's `new LineAccount`
   (`:326`): `createProxy` ×2 (`:331`, `:342`), router ctor + `govSet*` (`:336-338`), `setHookConfig` /
   `setGovernorAdmin` / `setInterestRateModel` / `setFeeReceiver` (`:332-348`), `submitCap`/`acceptCap`/
   `setSupplyQueue` (`:353-361`), `transferFrom`/`approve`/`deposit` (`:365-367`), `transferGovernance` (`:371`),
   `_assertWired` (`:374`). Each is in the SAME tx, so any revert unwinds the CREATE2 deploy too — confirm there
   is no `try/catch`, no low-level `call` that swallows a failure, and no step that emits an effect outside this
   tx's atomicity. (The I-9 precheck exists precisely because the EE onboarding could revert mid-cluster — is
   there any OTHER step with the same orphan risk that lacks a precheck?)
3. **The escrow vault hardening.** Step 1 builds the collateral vault then `setHookConfig(address(0), 0)`
   (`:332`) and `setGovernorAdmin(address(0))` (`:333`) — a bare holding box. **Confirm it has no oracle, no
   unit-of-account, no governance left** (so it can only hold the lien, never borrow / be re-governed). Diff vs
   how `EdgeFactory` configures its escrow.
4. **The lien custody path.** Step 5 (`:365-367`): `transferFrom(controller, this, 1e18)` → `approve(collat,
   1e18)` → `deposit(1e18, borrowAccount)`. Confirm the deposit credits the `borrowAccount` (not the adapter,
   not the controller), that the controller's origination batch is what authorizes the `transferFrom`, and that
   a partial/zero deposit is already blocked by the `!= 1e18` guard (`:316`) — an EVK `deposit(0,..)` does NOT
   revert (silent zero-share line), which is exactly why `!= 1e18` (not `!= 0`) is the guard.
5. **The `^ 1` borrow account.** `borrowAccount = address(uint160(la) ^ 1)` (`:327`) — sub-account 1 of the
   `LineAccount` prefix, code-free so the EVC non-owner-code-free guard doesn't trip. Confirm the adapter uses
   the SAME `borrowAccount` everywhere it acts on the line (draw `:457`, close `:534`) and that it matches what
   `LineAccount`'s ctor granted the operator bit over (the load-bearing XOR — see the `lineaccount/` drill).

## What is already proven (don't re-report as new)
`test_OpenLine_FullAmount_Succeeds`, `test_OpenLine_InvalidCollateralAmount_Zero`/`_Partial`,
`test_MarketWiring`, `test_LineAccount_Mechanics`, `test_TwoLine_DistinctPrefix_BothDraw_Isolation`,
`test_ForeignAccount_HookRejects`, `test_SEC08_TimelockPrecheck_RevertsEarly_NoOrphan`,
`test_SEC08_TimelockZero_HappyPath`, `test_SEC08_DeployProbe_PassesLive`/`_Bites`,
`test_DoubleOpenLine_SameLienId_Reverts`. Confirming the cluster mints atomically + isolation holds goes in your
Summary. A finding must be a concrete orphan / cross-line bleed / ordering bug the fork suite does NOT cover —
most valuably a delta from `EdgeFactory.deploy()` or a mid-cluster revert path the I-9 precheck doesn't catch.
